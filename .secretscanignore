# ─────────────────────────────────────────────────────────────────────────────
#  secret_scan.py allowlist — one Python regex per line. A finding is ignored
#  when its matched value matches any pattern here. Use sparingly, and prefer an
#  inline  # pragma: allowlist secret  comment for one-off lines.
# ─────────────────────────────────────────────────────────────────────────────

# Deterministic dev-only Garage S3 credentials shared by the compose stack and
# documented verbatim in .env.example (not real secrets). See the data-fabric
# notes for generating real values with `openssl rand`.
a1b2c3d4e5f60718293a4b5c6d7e8f90a1b2c3d4e5f60718293a4b5c6d7e8f90
GKa1b2c3d4e5f6708192a3b4c5

# Garage single-node rpc_secret (garage/garage.toml) — deterministic dev value,
# unused with replication_factor=1. Drive from env if you ever run multi-node.
0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# UI template-literal ternary that renders a data-secret="0|1" attribute flag
# (mcp/web_api config panels). Not a value — just marks which fields are secret.
\$\{f\.secret\?1:0\}
